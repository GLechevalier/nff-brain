{
  "name": "nff-brain-monorepo",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w nff-brain && npm run build -w nff-brain-vscode",
    "test": "vitest run",
    "typecheck": "tsc -p packages/core && tsc -p packages/cli && tsc -p packages/vscode"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "~5.6.3",
    "vitest": "^2.1.9"
  }
}
